- if projects.present?
  %h2= "#{title}"
  - projects.each do |project|
    %h3= "#{project.name} (#{project.progress} %)"

    = render partial: 'tasks/tasks_table', locals: {tasks: project.tasks, with_actions: false}
    %br